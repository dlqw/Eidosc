# Eidos Std 0.3.0-alpha.1 — read-helper ownership

- Borrow string inputs throughout hashing, binary conversion, JSON escaping, and JSON field comparisons instead of repeatedly consuming owned values.
- Read async sequence elements directly through shared references and rebuild compared `Duration` values from their consumed scalar fields.
- Keep the existing public by-value transformation contracts while making internal ownership flow independent of function bodies.
