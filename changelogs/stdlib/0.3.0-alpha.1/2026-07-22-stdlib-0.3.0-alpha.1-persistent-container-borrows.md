# Persistent container borrow observers

- Make `PersistentMap` and `PersistentSet` observers and updates accept `Ref` receivers.
- Preserve prior map and set versions while insert, remove, lookup, ordering, and sequence conversion traverse shared nodes.
