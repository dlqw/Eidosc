# Eidos Std 0.3.0-alpha.1 — hash collection ownership

- Rebuild `HashMap` lookup, replacement, removal, rehashing, membership, and folding as ownership-safe structural recursion with explicit key cloning.
- Rebuild `HashSet` toggle, symmetric difference, equality, subset, and filtering around single-owner traversal.
