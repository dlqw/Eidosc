# Eidos Std 0.3.0-alpha.1 — shared observer borrows

- Make `Shared.borrow` and `Shared.ptr_eq` take shared references so inspecting a shared handle never consumes it.
- Migrate persistent-map internals and native smoke coverage to pass explicit `ref` arguments.
