# Eidos Std 0.3.0-alpha.1 — network observer borrows

- Make HTTP request, text response, byte response, header, and status observers take shared references.
- Clone returned text and byte payloads explicitly so observing a response never consumes it or aliases owned storage.
- Migrate network helpers and regression fixtures to pass explicit `ref` arguments.
