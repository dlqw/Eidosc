# Eidos Std 0.3.0-alpha.1 — basic container observers

- Make `Deque`, `Queue`, `Stack`, and `SeqBuilder` length and emptiness queries take shared references.
- Evaluate consuming `peek` operations only after metadata reads, clone explicitly before consuming materialization, and migrate fixture calls to explicit `ref` arguments.
