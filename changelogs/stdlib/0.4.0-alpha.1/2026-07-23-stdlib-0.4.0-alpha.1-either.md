# Std 0.4.0-alpha.1：Either

- sync-id：`eidos-2026-07-23-implicit-unit-selection`
- 新增 `Std.Either.Either[L, R]`、`Left` 与 `Right`。
- 新增 right-biased `map`，以及 `map_left`、`bimap`、`fold`、`is_left`、`is_right`、`unwrap_or`。
- `Either` canonical identity 作为 `then` / `else` selection 的正式 subject 类型。
