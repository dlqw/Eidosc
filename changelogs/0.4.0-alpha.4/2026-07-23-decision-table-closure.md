# Decision table expression closure

- Added stable `W4301` diagnostics for duplicate literal and literal-tuple keys within one decision-table template group while preserving the ordered first-match semantics.
- Added focused formatter, type, effect-authorization, parser, and MIR-pipeline coverage for `decide fallback { template: rows }` expressions.
