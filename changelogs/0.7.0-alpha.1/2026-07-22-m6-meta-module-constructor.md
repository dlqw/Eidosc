# M6 typed generated module constructor

- Add the approved high-level `meta.module(name, items)` constructor for `meta.Package -> meta.Modules` extensions.
- Treat `meta.Modules` as the typed module sequence protocol and materialize only validated UpperCamelCase module paths with typed declaration items.
- Cover generated module origin and IDE virtual-document visibility.
