# M5 Meta query cache restore

- Verify repeated typed Meta queries produce an observable miss followed by a canonical cache hit.
- Verify cache entries and dependency history round-trip through module Types state and aggregate live-state payloads with valid fingerprints.
