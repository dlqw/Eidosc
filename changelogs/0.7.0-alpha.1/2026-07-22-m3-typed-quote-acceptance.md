# M3 typed quote acceptance

- Cover every public typed quote grammar category under the compiler-managed Meta surface.
- Verify quoted derive outputs retain deterministic output order, stable generated identity, origin data, virtual documents, and declaration-handle splice identity.
- Keep invalid non-syntax item splices as an explicit negative gate.
