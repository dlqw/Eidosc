# M6 package immutability

- Verify typed package extensions cannot replace or mutate existing declarations or modules.
- Reject colliding output batches atomically so earlier outputs in the same batch are not committed.
