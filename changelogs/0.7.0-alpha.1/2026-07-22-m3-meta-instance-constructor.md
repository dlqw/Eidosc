# M3 semantic instance constructor

- Expose the approved high-level `meta.instance(trait, target, methods)` constructor for typed derive output.
- Remove the interim `meta.implementation` builder name instead of retaining a compatibility alias.
