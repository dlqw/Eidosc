# Declaration attachment adapters

- Classify every bound declaration attachment by language responsibility: signature component, typed tag, foreign contract, dedicated declaration, compiler directive, or removed surface.
- Expose the versioned adapter groups from `DeclarationAttachmentIR` as the single classification source for parser, formatter, migrator, LSP, reflection, and metaprogram dispatch work.
