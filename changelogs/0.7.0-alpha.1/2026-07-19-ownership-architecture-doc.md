# Ownership architecture documentation

- Add the compiler/user responsibility contract for signature-derived ownership, Copy/Move/Clone, reborrow, partial move, escape, and drop-once behavior.
- Document the independent versioning and invalidation boundaries of `ownership-contract-v1` and `loan-summary-v1`.
- Document cache, reflection, IDE/LSP, diagnostics, and atomic migration requirements.
