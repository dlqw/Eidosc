# M3 generated identity cache gate

- Verify identical typed derive inputs reproduce the same generated slot and stable identity while explicit argument changes preserve the slot and invalidate the payload identity.
- Verify generated origin, argument fingerprint, schema version, and virtual document identity survive symbol cache payload serialization.
