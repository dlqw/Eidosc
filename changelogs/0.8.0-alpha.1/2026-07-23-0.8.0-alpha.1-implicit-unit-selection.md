# Eidosc 0.8.0-alpha.1：隐式 Unit body 与分支选择

- sync-id：`eidos-2026-07-23-implicit-unit-selection`
- lexer/parser/AST/name/types/effects 增加 selection 与 arm-local `_N` synthetic binding。
- HIR 将 selection 降低为普通 `match`，MIR/LLVM 不新增语法糖节点。
- 函数普通 block body 在首个规范化运行时参数为 `Unit` 时降低为唯一 wildcard branch。
- formatter 输出无冗余花括号的单表达式 arm，并保持多语句 block。
- LSP 增加 placeholder 类型 hover、semantic token、definition、completion 与 arm-local visibility，并明确不开放 placeholder rename。
- 增加经过 parse/name/type preview 验证的 selection → `match`（`S1005`）和 canonical 双臂 `match` → selection（`S1006`）保守 code action。
- 增加 canonical identity、group、诊断、CTFE、closure、HIR/MIR、generated source map 与 formatter 回归测试。
