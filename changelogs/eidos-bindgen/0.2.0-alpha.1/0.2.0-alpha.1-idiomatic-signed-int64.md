# Eidosc.Bindgen 0.2.0-alpha.1 signed 64-bit integer mapping

- Generate Eidos `Int` for C `int64_t`, whose ABI is the same i64 representation, so ordinary integer literals, traits, and core APIs compose with generated bindings without redundant conversions.
- Keep unsigned and other explicit fixed-width boundary mappings distinct.
