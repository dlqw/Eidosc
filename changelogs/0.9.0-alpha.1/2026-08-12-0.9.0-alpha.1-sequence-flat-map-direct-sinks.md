# Sequence flat_map direct sinks

- Fuse `flat_map` directly into `find`, `any`, `all`, and `for_each` when callback purity, single-use, ownership, and non-escape proofs hold.
- Lower the fused form to nested outer/inner loops with source-order callback evaluation, short-circuit exits, and exactly one cleanup of each mapper-produced inner sequence.
- Unknown or unsafe ownership facts continue to use the existing eager implementation.
