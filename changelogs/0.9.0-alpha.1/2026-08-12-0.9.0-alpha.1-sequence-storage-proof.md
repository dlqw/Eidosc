# Sequence storage proof integration

- Runtime sequence storage promotion now consumes `OwnershipAnalysisSnapshot` facts for the allocation point.
- Inline storage is retained only when the candidate is owned, unique, non-aliased, non-borrowed, and non-escaping; unknown snapshots conservatively keep the existing local analysis route.
- Unified sequence metadata now treats incomplete exit cleanup as an ownership-proof failure.
