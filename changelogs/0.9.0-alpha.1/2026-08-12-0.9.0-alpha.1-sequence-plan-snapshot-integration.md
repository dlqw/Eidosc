# 0.9.0-alpha.1

## Compiler

- 补齐 ownership snapshot 的退出清理、partial-move/reinitialize 与 drop-exactly-once 事实及测试入口。
- MIR optimizer 按模块身份构建 `OwnershipAnalysisSnapshot` 并注入 sequence fusion proof；snapshot 不可用时保持安全回退。
- 新增统一 `SequencePlan` 的 Source/Stages/Sink/OwnershipRoute/StoragePlan/ProofSummary/OriginSpans 元数据骨架，先接入 `filter → head/find`。
