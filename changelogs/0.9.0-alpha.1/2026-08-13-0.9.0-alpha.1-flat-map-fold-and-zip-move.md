# Flat-map fold and move-aware zip routes

- Added proof-gated `flat_map → fold_left` fusion with nested source-order loops and one cleanup per mapper-produced inner sequence.
- Zip direct `find` now permits non-Copy pair fields when the callback uses a shared borrow; source loads are marked destructive on that route.
- Copy-only sinks retain their conservative fallback when move/drop facts are incomplete.
