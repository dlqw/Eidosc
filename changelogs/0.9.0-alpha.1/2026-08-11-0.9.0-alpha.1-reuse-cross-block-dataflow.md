# Reuse 跨基本块数据流分析

- 将 `ReuseAnalyzer` 统一为跨基本块 must-dataflow：join 点只接受所有前驱都可用的复用槽。
- 支持前驱 block 的 drop 与后继 block 构造器配对，并允许同一上游 drop 在互斥分支中安全共享复用槽。
- 对路径不完整、Perceus 已省略的 drop 以及循环回边保持保守回退，避免错误的 `drop_reuse`/`alloc_reuse` 配对。
