# Analyze FFI configuration

- Fixed `eidosc analyze` to propagate the resolved project and package FFI configuration into the compilation pipeline, so structured extern library identities are validated against `eidos.toml [ffi].libraries` consistently with build and run commands.
