# IDE/LSP source-coordinate and code-action correctness

- Derive all semantic snapshot ranges from absolute source offsets with UTF-16, LF, CRLF, and multiline support.
- Emit source code actions and semantic renames as versioned `TextDocumentEdit` operations, including insertion and deletion edits.
- Publish selection/match migrations as explicit `refactor.rewrite` actions instead of oscillating diagnostics.
- Preserve exact import insertion coordinates and document line endings.
