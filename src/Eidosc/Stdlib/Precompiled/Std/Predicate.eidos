Predicate :: module {
    import Trait
    import TraitInvoke

    accept[A, E: effects] :: (A -> Bool need E) -> A -> Bool need E
    {
        predicate => value => predicate(value)
    }

    reject[A, E: effects] :: (A -> Bool need E) -> A -> Bool need E
    {
        predicate => value => !predicate(value)
    }

    is[A: Trait::Eq] :: A -> A -> Bool
    {
        expected => value => value == expected
    }

    is_not[A: Trait::Eq] :: A -> A -> Bool
    {
        expected => value => !(value == expected)
    }

    both[A: Trait::Clone, E1: effects, E2: effects] :: (A -> Bool need E1) -> (A -> Bool need E2) -> A -> Bool need E1, E2
    {
        left => right => value => {
            copied := TraitInvoke::clone_value(value);
            if left(value) then { right(copied) } else { false }
        }
    }

    either[A: Trait::Clone, E1: effects, E2: effects] :: (A -> Bool need E1) -> (A -> Bool need E2) -> A -> Bool need E1, E2
    {
        left => right => value => {
            copied := TraitInvoke::clone_value(value);
            if left(value) then { true } else { right(copied) }
        }
    }
}
